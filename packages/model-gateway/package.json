{
  "name": "@ezu/model-gateway",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm build && node --test \"dist/model-gateway/src/*.test.js\""
  },
  "dependencies": {
    "@ezu/protocol": "workspace:*"
  }
}
